rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    // Default-deny catch-all safety net
    match /{document=**} {
      allow read, write: if false;
    }

    // Helper functions
    function isSignedIn() {
      return request.auth != null;
    }

    function isUserOwner(userId) {
      return isSignedIn() && request.auth.uid == userId;
    }

    match /users/{userId} {
      allow read: if true;
      allow create, update: if isUserOwner(userId) || !isSignedIn();
    }

    match /posts/{postId} {
      allow read: if true;
      allow create: if (isSignedIn() && request.resource.data.userId == request.auth.uid) || !isSignedIn();
      allow update: if true; // Allowed for reactions (smile, love, support, inspiring) and commentsCount incrementing
      allow delete: if (isSignedIn() && resource.data.userId == request.auth.uid) || !isSignedIn();
    }

    match /comments/{commentId} {
      allow read: if true;
      allow create: if (isSignedIn() && request.resource.data.userId == request.auth.uid) || !isSignedIn();
      allow delete: if (isSignedIn() && resource.data.userId == request.auth.uid) || !isSignedIn();
    }
  }
}
